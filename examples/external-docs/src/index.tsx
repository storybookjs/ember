/* global document */

import React from 'react';
import ReactDOM from 'react-dom';
import StoriesPage from './StoriesPage';

const App = () => <StoriesPage />;

ReactDOM.render(<App />, document.getElementById('root'));
