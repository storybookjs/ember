```js
// Button.stories.js

import Button from './Button.vue';

export default {
  component: Button,
};

export const Text = {
  render: ({ label }) => ({
    components: { Button },
    setup() {
      return {
        label,
        onClick: action('clicked'),
      };
    },
    template: '<Button :label="label" @click="onClick" />',
  }),
};
```
