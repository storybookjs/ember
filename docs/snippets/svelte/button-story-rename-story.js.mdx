```js
// Button.stories.js

import Button from './Button.svelte';

export default {
  component: Button,
};

export const Primary = ({
  render: () => ({
    Component: Button,
    props: {
      primary: true,
      label: 'Button',
    },
  }),
};

Primary.storyName = 'I am the primary';
```
