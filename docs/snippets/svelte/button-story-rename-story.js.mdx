```js
// Button.stories.js

import Button from './Button.svelte';

export default {
  component: Button,
};

export const Primary = ({
  name: 'I am the primary',
  render: () => ({
    Component: Button,
    props: {
      primary: true,
      label: 'Button',
    },
  }),
};
```
