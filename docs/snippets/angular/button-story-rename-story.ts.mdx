```ts
// Button.stories.ts

import { Button } from './button.component';

export default {
  component: Button,
};

export const Primary = {
  name: 'I am the primary',
  render: () => ({
    props: {
      label: 'Button',
      primary: true,
    },
  }),
};
```
